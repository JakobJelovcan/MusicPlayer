<UserControl
    x:Class="MusicPlayerLibrary.Controls.Buttons.PlayPauseButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MusicPlayerLibrary.Controls.Buttons"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:DefaultBindMode="OneWay"
    Height="32"
    Width="96"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="100">
    <Button Click="Button_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" Spacing="10">
            <TextBlock Text="{x:Bind ButtonText, FallbackValue=Play}" VerticalAlignment="Center"/>
            <TextBlock Text="{x:Bind ButtonSymbol, FallbackValue=&#xF5B0;}" FontFamily="Segoe MDL2 Assets" VerticalAlignment="Center"/>
        </StackPanel>
    </Button>
</UserControl>
